CREDENTIAL_ROOM - SOLUTION

This web application is vulnerable to sql injection attacts.

We can input "' OR '1'='1" into the username and password fields to bypass the vulnerable authentication.

This input modifies the SQL query to return true for every row, effectively bypassing the login check.
